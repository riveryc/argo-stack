apiVersion: v1
kind: Service
metadata:
  name: app-validation-service-preview
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: app-validation-service