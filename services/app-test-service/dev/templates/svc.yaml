apiVersion: v1
kind: Service
metadata:
  name: {{ required (printf "%s missing" .Values.serviceName) .Values.serviceName }}
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ required (printf "%s missing" .Values.serviceName) .Values.serviceName }}